fun main() {
    var count = 0
    var bongo = 1 // Abaixo estrutura de condicionais
    if (bongo==0) println("Poxa, Olodum sem bongô? Como assim?")
    else while(count <=64) { // Estrutura de repetição
        print("\ttuts tats")
        count++
    }
}